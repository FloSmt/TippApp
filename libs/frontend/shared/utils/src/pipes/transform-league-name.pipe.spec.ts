import { TransformLeagueNamePipe } from './transform-league-name.pipe';

describe('TransformLeagueNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformLeagueNamePipe();
    expect(pipe).toBeTruthy();
  });
});
