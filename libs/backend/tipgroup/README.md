# tipgroup

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tipgroup` to build the library.

## Running unit tests

Run `nx test tipgroup` to execute the unit tests via [Jest](https://jestjs.io).
