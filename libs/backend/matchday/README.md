# matchday

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build matchday` to build the library.

## Running unit tests

Run `nx test matchday` to execute the unit tests via [Jest](https://jestjs.io).
