# tipseason

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tipseason` to build the library.

## Running unit tests

Run `nx test tipseason` to execute the unit tests via [Jest](https://jestjs.io).
