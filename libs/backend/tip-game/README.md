# tip-game

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tip-game` to build the library.

## Running unit tests

Run `nx test tip-game` to execute the unit tests via [Jest](https://jestjs.io).
