name: 🔄 Nx Migration Check & Update

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 6'

permissions:
  contents: write
  actions: write
  pull-requests: write

jobs:
  migrate-dependencies:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [ 22.x ]

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 🛠️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: 📦 Install Dependencies
        run: npm install

      - uses: nrwl/nx-set-shas@v4

      - name: 🚀 Run Nx Migration Check and Execution
        id: migration
        env:
          NX_CLOUD_ACCESS_TOKEN: ${{ secrets.NX_CLOUD_ACCESS_TOKEN }}
        run: |
          echo "::group::Check for Migrations"
          npx nx migrate latest
          echo "::endgroup::"

          if git status --porcelain | grep -q 'package.json\|migrations.json'; then
            echo "migration_needed=true" >> $GITHUB_OUTPUT
            echo "::group::Run Migrations"
            npx nx migrate --run-migrations
            echo "::endgroup::"
          else
            echo "migration_needed=false" >> $GITHUB_OUTPUT
            echo "Keine Migrationen notwendig."
          fi

      - name: 📤 Create Pull Request (PR)
        if: steps.migration.outputs.migration_needed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: 'chore: Automated Nx Migration Update'
          title: '🤖 Nx Migration Update'
          body: |
            Dieser Pull Request wurde automatisch durch den `Nx Migration Check` Workflow erstellt.

            Die folgenden Schritte wurden ausgeführt:
            * `nx migrate latest`
            * `nx migrate --run-migrations`

            Bitte prüfen und mergen.
          branch: 'dependency-updates/automated-nx-migration'
          delete-branch: true
